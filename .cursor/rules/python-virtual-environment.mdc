---
alwaysApply: true
---

# Python虚拟环境规范

## 虚拟环境要求

ATMQuant项目的所有Python代码执行都必须在激活的vnpy_env虚拟环境下运行。

### 虚拟环境路径
- 虚拟环境位置: [vnpy_env/](mdc:vnpy_env/)
- 激活脚本: `vnpy_env/bin/activate`

### 执行规范

1. **命令行执行**
   - 所有Python脚本执行前必须先激活虚拟环境
   - 使用命令: `source vnpy_env/bin/activate`
   - 然后执行Python命令: `python script_name.py`

2. **终端操作**
   - 在运行任何Python相关命令前，确保虚拟环境已激活
   - 激活后终端提示符会显示 `(vnpy_env)`

3. **脚本执行示例**
   ```bash
   # 激活虚拟环境
   source vnpy_env/bin/activate
   
   # 执行Python脚本
   python main.py
   python scripts/download_futures_data.py
   python run_tests.py
   
   # 运行测试
   pytest tests/
   
   # 安装依赖
   pip install -r requirements.txt
   ```

4. **项目相关文件**
   - 主入口: [main.py](mdc:main.py)
   - 依赖文件: [requirements.txt](mdc:requirements.txt)
   - 测试配置: [pytest.ini](mdc:pytest.ini)
   - 测试运行器: [run_tests.py](mdc:run_tests.py)

### 重要提醒

- 虚拟环境配置文件: [vnpy_env/pyvenv.cfg](mdc:vnpy_env/pyvenv.cfg)
- 所有vnpy相关的Python包都安装在此虚拟环境中
- 不激活虚拟环境可能导致依赖包缺失或版本冲突
- 确保开发环境的一致性和稳定性

### 检查虚拟环境状态

```bash
# 检查当前Python解释器路径
which python

# 应该返回类似: /Users/mac/code/atmquant/vnpy_env/bin/python
```